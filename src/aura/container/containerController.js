({
    init: function(cmp, event, helper) {
        helper.initializeValues(cmp);
    },

    handleParameterChange: function(cmp, event, helper) {
        helper.handleParameterChange(cmp, event);
    },
})
