({
    init: function(cmp, event, helper) {
    },

    onLoad: function(cmp, event, helper) {
        helper.onLoad(cmp, event);
    },

    onError: function(cmp, event, helper) {
        helper.onError(cmp, event);
    },
})
