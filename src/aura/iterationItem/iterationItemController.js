({
    init: function(cmp, event, helper) {
        helper.handleItemsChange(cmp, event);
    },

    handleItemsChange: function(cmp, event, helper) {
        helper.handleItemsChange(cmp, event);
    },

    renderItem: function(cmp, event, helper) {
        helper.renderItem(cmp, event);
    },

    getItemUI: function(cmp, event, helper) {
        return helper.getItemUI(cmp, event);
    },
})
