({
    init: function(cmp, event, helper) {
    },

    renderItems: function(component, event, helper) {
        helper.renderItems(component, event);
    },

    getItemUIs: function(component, event, helper) {
        return helper.getItemUIs(component, event);
    },
})
