({
    init: function(cmp, event, helper) {
        helper.handleValueChange(cmp, event);
    },

    handleValueChange: function(cmp, event, helper) {
        helper.handleValueChange(cmp, event);
    },

    handleOptionClick: function(cmp, event, helper) {
        helper.handleOptionClick(cmp, event);
    },

    handleMouseOver: function(cmp, event, helper) {
        helper.handleMouseOver(cmp, event);
    },

    handleMouseOut: function(cmp, event, helper) {
        helper.handleMouseOut(cmp, event);
    },
})
